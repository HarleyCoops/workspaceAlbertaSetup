# Shell scripts must be LF. A CRLF shebang makes the file unexecutable on Linux
# ("/usr/bin/env: 'bash\r': No such file or directory"), which is fatal for an
# installer that only ever runs on Raspberry Pi and Ubuntu.
*.sh        text eol=lf
*.desktop   text eol=lf
*.service   text eol=lf
