# Copy to sdkconfig.credentials (gitignored) on the flash machine only.
#   cp sdkconfig.credentials.example sdkconfig.credentials
# Then paste secrets locally. Do not commit sdkconfig.credentials.
#
# Christian: generate a reusable auth key for tailnet harleycoops.github at
# https://login.tailscale.com/admin/settings/keys
# Approve the new node (wa-esp32-amoled) in the Tailscale admin if asked.

CONFIG_WA_WIFI_PASSWORD=""
CONFIG_WA_TS_AUTH_KEY=""
CONFIG_ML_TAILSCALE_AUTH_KEY=""
CONFIG_ML_WIFI_PASSWORD=""
